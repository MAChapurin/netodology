/**
 * Возвращает ссылку на карту указанной в компоненте страны
 */

export function MapBlock({country}) {
    return (
        <div>
            <a href="#!">здесь ссылка на карту {country}</a>
        </div>
    )
}